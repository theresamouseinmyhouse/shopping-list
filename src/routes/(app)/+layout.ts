// The app is a client-rendered PWA shell (Dexie, EventSource, localStorage).
// The server still runs hooks (auth gate) and the /api routes.
export const ssr = false;
export const prerender = false;
